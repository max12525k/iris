name: iris

include:
  - ./iris/compose.yaml
  - ./litellm/compose.yaml
  - ./honcho/compose.yaml
  - ./claude-cli/compose.yaml
  - ./observability/compose.yaml      # V2 Plane 1: OTel + Loki + Promtail + Grafana
  - ./iris/profiles.compose.yaml      # V2 Plane 2: opt-in profile gateways (researcher/coder/ops)

networks:
  frontend:
    driver: bridge
  backend:
    driver: bridge
    internal: true
  data:
    driver: bridge
    internal: true
